import java.util.Arrays;
import java.util.List;

public class Constants {

     // Burada Assert kontrollerim için sabitler tanımladım.
     public static final List<Boolean> installmentIs1 = Arrays.asList(true,true,true);
     public static final List<Boolean> installmentIs2 = Arrays.asList(false,false,false);


}
